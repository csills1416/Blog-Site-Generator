<!DOCTYPE html>
<html>
<head>
  <title>Tech Blog - {{ post.title }}</title>
</head>
<body>
  <header>
    <h1>Welcome to Tech Blog</h1>
    {{#if user}}
      <a href="/dashboard">Dashboard</a>
      <a href="/logout">Logout</a>
    {{else}}
      <a href="/login">Login</a>
    {{/if}}
  </header>
  
  <main>
    <article class="blog-post">
      <h2>{{ post.title }}</h2>
      <p>{{ post.content }}</p>
      <p>By {{ post.user.name }}</p>
      <p>Date: {{ post.createdAt }}</p>
    </article>
    
    <section class="comments-section">
      <h3>Comments</h3>
      {{#each post.Comments}}
        <div class="comment">
          <p>{{ this.content }}</p>
          <p>By {{ this.user.name }}</p>
          <p>Date: {{ this.createdAt }}</p>
        </div>
      {{/each}}
    </section>
  </main>
</body>
</html>
